Like we did for synthetizer ahead of mock, I would like to add a synthetizer just ahead of architecture. Very similar, but geared toward architecture. Let's apply same rule: if the size of the output is bigger than the sum of the intrant, let's use the originals. Let's also add a synthetizer before the build plan, which should be built from the 2 previous synthetized. Ensure to update the visual workflow and editable prompts.
