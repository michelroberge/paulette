I would like to add a new feature. I would like to allow defining the mode of operation for the AI. Options are RAG or Files. RAG option is available only if RAG is configured in .env. Default is Files. This can be changed per project in project's settings. When using "files", we should generate a .paulette/prompts directory in the target repo, and write ALL the prompt templates we will use there, and use THESE dynamically, instead of the ones written within the code of this go app. I also want to render a visual logical view of our workflow in a graphical way, including the loops, where the user can click a node, see the prompt, and modify it. The goal being yes, we do have a "hard deterministic" process, but every prompt can be tuned per project.
