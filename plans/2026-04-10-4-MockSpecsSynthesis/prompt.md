I want to add a new step between UX Design and mock generation. I want to add a step that will ask the AI to synthesize the vision and the UX design into a single specs document for mock generation. The output artifact of this step will be the ONLY artifact sent to mock generation, which should in theory be smaller than the sum of the 2 other artifacts. If the resulted size is bigger, then send the 2 artifacts instead.
