# Prompt

Here is a list of bugs for you to fix.

1. In architecture step, the Generate architecture takes the whole width. It should be just below Send button and say "Generate".
2. When we go to build on mobile, we don't see the box to send messages so we cannot interact. In mobile, we cannot select text and trigger fine tune with AI and manually edit. It should support user holding a word, and bring the paragraph in the edit.
3. Build step does not remember which sub step we were. It should, it makes no sense to have to click next many times again and re-trigger generating beads.
4. Build step got stuck on implement and I see 2 dancing paulette. This makes no sense. When I go to generate beads, it should not regenerate the beads if they were already generated. Just load them. Implement step should just display the beads, either graphical or list (depending if desktop or mobile). Right now I see 2 dancing paulette, and if AI connection fails, I don't know.

Follow up: fix all in autonomous mode, then commit and push.
