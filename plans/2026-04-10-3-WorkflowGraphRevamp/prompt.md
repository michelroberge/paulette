I see a few problems I'd like to address. They might just be visual, or they might be showing a problem with our flow. First, let's make it more like a cascade instead of horizontal line. 2nd, each main step (Vision, UX Design, Build Plan) is supposed to be a loop. Vision should be looping on "is vision clear enough and approved?", and I believe there should be a "refinement" prompt, and a branching (generate artifact and go to next step, or loop). UX Design should also have the loop and branch, + the step to generate the mocks. Architecture also has the loop and branch. Finally, the devil advocate also has a branch, and the build loop has a branch. I want these to be represented on the visual graph. Finally, when I hover a step, it highlights, if I click it as well, but if I move the mouse away, I don't see which one is selected. It is important to keep the selected node highlighted. Finally, it would be great if we were showing the included context artifacts at the top of the template, something like Additional Context: "vision.md", "ux-design.md". Finally, we must also include the guided vs free mode vision flow, so I suggest the 1st block to be "begin" with no prompt, a decision branch (guided vs free), and then the 2 path, and they join back after the vision steps.
