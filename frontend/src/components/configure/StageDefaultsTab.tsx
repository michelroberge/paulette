/**
 * StageDefaultsTab — global per-stage connection and model assignment table.
 *
 * Implemented in task 5.4. This stub file exists so that ConfigurePage.tsx (task 5.1)
 * compiles while the full component is being developed.
 *
 * Journey: JRN-v0.2.0-006
 * Architecture: ARCH-v0.2.0-025, SCR-010, IACT-010
 */

import type { CSSProperties } from 'react';

export function StageDefaultsTab() {
  const placeholderStyle: CSSProperties = {
    padding: '4rem 0',
    textAlign: 'center',
    color: '#64748b',
    fontSize: '0.9375rem',
  };

  return (
    <div style={placeholderStyle}>
      <p>Stage Defaults tab — coming in task 5.4.</p>
    </div>
  );
}
